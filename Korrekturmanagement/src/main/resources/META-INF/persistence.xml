<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="KorrekturMgrPU" transaction-type="JTA">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- WildFly Datasource -->
        <jta-data-source>java:/korrekturDB</jta-data-source>

        <!-- Entities -->
        <class>korrekturmanagement.model.SqlDataKorrekturmgm</class>
        <class>korrekturmanagement.model.User</class>

        <!-- Optional: Shared Cache Mode -->
        <shared-cache-mode>ENABLE_SELECTIVE</shared-cache-mode>

        <!-- Optional: Validation Mode -->
        <validation-mode>AUTO</validation-mode>

        <!-- Hibernate Properties -->
        <properties>
            <property name="hibernate.dialect" value="org.hibernate.dialect.MariaDBDialect"/>
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>

</persistence>
